const mobile = "(max-width: 768px)";

const tablet = "(min-width: 769px) and (max-width: 1199px)";

const desktop = "(min-width: 1200px)";

const dark = "(prefers-color-scheme: dark)";

const light = "(prefers-color-scheme: light)";

export {
    dark,
    light,
    mobile,
    tablet,
    desktop,
};
